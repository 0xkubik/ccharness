---
description: "Work out HOW to build a picked direction (or resolve a standalone fork) — four opposed proposers (MVP / Final / Conventional / Contrarian) argue different ways to build it, get grilled against each other, and converge on ONE buildable approach. Coming from /what-to-do it decides the *how*, not *whether* — that pick was what-to-do's; if the pick itself looks wrong it flags you rather than silently overriding. A structured, depth-scaled replacement for open-ended brainstorming. The approach flows on to /do to build; you redirect at the boundary if needed."
argument-hint: "<decision or question>"
---

Invoke the `how-to-do` skill (the decision loop) with this question:

> $ARGUMENTS

Grill-it triages the stakes first: reversible & low-stakes questions take the fast path (one
proposal), while irreversible/expensive ones run the full grill — four partisan proposers arguing
different ways to build it → cross-examination → a single synthesized approach that flows straight
into `/do` to build, with the door open for you to redirect. Coming from `/what-to-do`, it
works out *how* to build what you picked — it does not re-pick the direction; if the pick itself
looks wrong it flags you rather than silently overriding.
