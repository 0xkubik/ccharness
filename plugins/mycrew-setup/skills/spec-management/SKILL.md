---
name: spec-management
description: "Use for the project's specs — detailed feature and contract descriptions in docs/specs/, where a feature's one-liner is not enough. Placeholder: the rules are not written yet."
argument-hint: "<the spec to shape or grow>"
---

# spec-management — the specs, as living documents

Not written yet. Specs live in `docs/specs/` — detailed descriptions (a feature's mechanics, a
contract, a data schema) where a `features.md` one-liner is not enough. The rules for shaping them
come later.
