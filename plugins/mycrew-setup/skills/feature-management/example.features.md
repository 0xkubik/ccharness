# Features — <project name>

<!-- Template for docs/features/features.md — the project's declarative feature state: the code is
     pulled toward this list. `- [ ]` = not built yet, `- [x]` = built. Built features stay checked
     forever — a line is never deleted; the list only accumulates. -->

- [ ] <feature the user affirmed — one line, ≤200 chars>
- [ ] <next feature>
- [x] <a feature already built — stays here forever>
