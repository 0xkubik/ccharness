# <product> — Product Features

<!-- The product's own declarative state: cross-cutting capabilities that span sub-projects — one
     level ABOVE the repos' feature files. A line here is a product capability (touching more than one
     sub-project); the chief decomposes it into per-repo features. NOT repo features, chores, or bugs —
     those live in each sub-project's own docs/features/features.md. The North Star is NOT here — it
     lives in the product CLAUDE.md. Flat list; first unchecked [ ] = next; a shipped line is marked
     [x] and never deleted. -->

1. [ ] <product-level capability — one line>
2. [x] <a capability already shipped — stays here forever>
